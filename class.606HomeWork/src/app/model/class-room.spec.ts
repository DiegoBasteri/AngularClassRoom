import { ClassRoom } from './class-room';
import { ClassRoomModel } from './class-room';

describe('ClassRoom', () => {
  it('should create an instance', () => {
    expect(new ClassRoom()).toBeTruthy();
  });
});
